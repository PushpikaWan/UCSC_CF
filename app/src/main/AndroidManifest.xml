<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pushpika.ucsc_cf">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EmailAddressEntryActivity" />
        <activity android:name=".FloorsActivity" />
        <activity android:name=".FloorAActivity" />
        <activity android:name=".FloorAMapActivity" />
        <activity android:name=".FloorBActivity" />
        <activity android:name=".FloorBMapActivity" />
        <activity android:name=".FloorCActivity" />
        <activity android:name=".FloorCMapActivity" />
        <activity android:name=".FloorDActivity" />
        <activity android:name=".FloorDMapActivity"></activity>
    </application>

</manifest>